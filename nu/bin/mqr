#!/usr/bin/env zsh

set -o pipefail

default_shard=s0
default_env=prod

shard=${1:-$default_shard}
env=${2:-$default_env}

nu mordor query ${1:-$default_shard} --env ${2:-$default_env} - --accept application/json
