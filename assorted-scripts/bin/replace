#!/bin/bash
#
# Find and replace by a given list of files.
#
# replace foo bar **/*.rb

set -euxo pipefail

find_this="${1}"
echo $find_this
shift
replace_with="${1}"
echo $replace_with
shift

echo $*

ag -l ${find_this} $* | xargs sed -i '' "s/${find_this}/${replace_with}/g"
