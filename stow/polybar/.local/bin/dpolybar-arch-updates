#!/usr/bin/env bash

set -euo pipefail

exec 2> /dev/null

function list_updates {
  paru -Qu
}

updates="$(list_updates)"

status="$?"

if [[ $status -eq 0 ]] && [[ -n $updates ]]; then
  echo "$status" "$updates" | wc -l
else
  echo ''
fi

