#!/usr/bin/env bash

function list_updates {
  /usr/bin/checkupdates
  /usr/bin/paru -Qqu
}

updates="$(list_updates)"

if [[ $? != 0 ]]; then
  echo ''
elif [[ -n $updates  ]]; then
  echo "$updates" | wc -l
else
  echo ''
fi

