#!/usr/bin/env bash

set -euo pipefail

screenshots_dir="${HOME}/Pictures/Screenshots"
file_location="${screenshots_dir}/Screenshot-%F_%H_%M_%S_%z.png"

mkdir -p "${screenshots_dir}"

scrot "$@" "${file_location}" --exec 'dscrot-notify $f'

