#!/usr/bin/env zsh

set -euo pipefail

hostname=${HOSTNAME:-$(hostname)}
packages=$(cat "${HOUDINI_DIR}/hosts/${hostname}")

echo $packages |
  sort |
  xargs stow --restow --verbose=2 --target $HOME
