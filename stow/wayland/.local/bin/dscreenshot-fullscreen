#!/usr/bin/env bash

set -euo pipefail

file_location=$(dscreenshot-filename)

if [[ -n "${WAYLAND_DISPLAY:-}" ]]; then
  grim "${file_location}"
else
  maim "${file_location}"
fi

dscreenshot-notify "${file_location}"
